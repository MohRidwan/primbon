1. download ubuntu 14.04 LTS
2. Buat flashdisk installer dengan Rufus
3. Boot
4. Install Ubuntu
5. English, Continue
6. Something else, Continue
7. Free Space, +
8. Untuk root = kapasitas 87,5%
   Type for the new partition : primary
   Location..... : Begininng of this space
   Use as : Ext4 journaling file system
   Mount point : /
   OK
9. Untuk swap = kapasitas 12,5%
   Type for the new partition : logical
   Location..... : Begininng of this space
   Use as : swap area
   OK
10. Install now, Continue
11. Where Are you : Jakarta, Continue
12. Keyboard layout : English (US), Continue
13. isi username & pwd ext:bayu,pendaftaran,rendeh, Continue
14. Restart now

15. Install wine 4 stable : http://tipsonubuntu.com/2019/02/01/install-wine-4-0-ubuntu-18-10-16-04-14-04/
    - 1. Open terminal from application menu, then run command to enable 32 bit architecture:

        sudo dpkg --add-architecture i386

    - 2. Run command to download and add the repository key:

        wget -nc https://dl.winehq.org/wine-builds/winehq.key

        sudo apt-key add winehq.key

    - 3. For Ubuntu 18.04, Linux Mint 19.x, run command to add the repository:

        sudo apt-add-repository 'deb https://dl.winehq.org/wine-builds/ubuntu/ bionic main'

        For other releases, replace bionic in the code with:

        cosmic for Ubuntu 18.10.
        xenial for Ubuntu 16.04, and Linux Mint 18.x.
        trusty for Ubuntu 14.04.

        *ex : seusikan versi ubuntu ganti bionic -> trusty untuk Ubuntu 14.04

    - 4. Finally check system package cache and install Wine 4.0:

        sudo apt-get update

        sudo apt-get install --install-recommends winehq-stable

16. Cek versi wine : wine --version
17. Wine Configuration : winecfg

18. Install PlayOnLinux
    - Open Ubuntu Software Center
    - Search playonlinux, install
    - install winbind : sudo apt-get install winbind
    - install office 2010

19. Install xampp
    - Download versi 1.8.3-5
    - sudo su
    - chmod +x xampp-linux-x64–7.1.22–0-installer.run
    - ./xampp-linux-x64–7.1.22–0-installer.run

20. Autostart xampp : https://www.computernetworkingnotes.com/linux-tutorials/auto-start-xampp-at-startup-in-ubuntu-linux.html
    - sudo gedit /etc/init.d/lampp
    - In opened file, add the following lines.

        #!/bin/bash
        ### BEGIN INIT INFO
        # Provides: lampp
        # Required-Start:    $local_fs $syslog $remote_fs dbus
        # Required-Stop:     $local_fs $syslog $remote_fs
        # Default-Start:     2 3 4 5
        # Default-Stop:      0 1 6
        # Short-Description: Start lampp
        ### END INIT INFO
        /opt/lampp/lampp start

    - Save it
    - Close the opened file and run the following commands to make it executable.

        $ls –l /etc/init.d/lampp
        $sudo chmod +x /etc/init.d/lampp
        $ls –l /etc/init.d/lampp
    
    - Now run the following command.

        $sudo update-rc.d lampp defaults
        This command instructs Ubuntu to run our script file in all default runlevels.

    - reboot
    
    - Mengatasi Access forbidden !
      1. sudo gedit /opt/lampp/etc/extra/httpd-xampp.conf
      2. pada #New XAMPP security concept ganti/comment :
         Require lokal -> Require all granted
      3. Save, Close
      4. sudo /opt/lampp/lampp restart
      5. isi password di phpmyadmin untuk security
      6. edit password gunakan SQLyog

     - ubah permission lampp jadi open: sudo chmod 777 -R /opt/lampp
       copas repo
     - ubah permission lampp kembali jadi closed: sudo chmod 755 -R /opt/lampp    

21. Install SQLyog
    - Open PlayOnLinux
    - Search SQLyog
    - Install

22. instal firefox 21(sesuaikan versi)
    - wget sourceforge.net/projects/ubuntuzilla/files/mozilla/apt/pool/main/f/firefox-mozilla-build/firefox-mozilla-build_39.0.3-0ubuntu1_amd64.deb
    - sudo dpkg -i firefox-mozilla-build_39.0.3-0ubuntu1_amd64.deb