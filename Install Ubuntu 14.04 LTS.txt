1. download ubuntu 14.04 LTS
2. Buat flashdisk installer dengan Rufus
3. Boot
4. Install Ubuntu
5. English, Continue
6. Delete and isntall ubuntu
7. Install now, Continue
8. Where Are you : Jakarta, Continue
9. Keyboard layout : English (US), Continue
10. isi username & pwd ext:bayu,pendaftaran,rendeh, Continue
11. Restart now
12. Update & Upgrade ubuntu ke repo local
    - nano /etc/apt/sources.list
    - komen semua default repo ganti dengan :
        deb ftp://ftp.itb.ac.id/pub/ubuntu trusty main restricted universe multiverse
        deb ftp://ftp.itb.ac.id/pub/ubuntu trusty-updates main restricted universe multiverse
        deb ftp://ftp.itb.ac.id/pub/ubuntu trusty-security main restricted universe multiverse
        deb ftp://ftp.itb.ac.id/pub/ubuntu trusty-backports main restricted universe multiverse
        deb ftp://ftp.itb.ac.id/pub/ubuntu trusty-proposed main restricted universe multiverse
13. Instal wine : sudo apt-get install wine
14. Instal playonlinux : sudo apt-get install playonlinux
15. install winbind : sudo apt-get install winbind
16. Install xampp
    - Download versi 1.8.3
    - sudo su
    - chmod +x xampp-linux-x64–7.1.22–0-installer.run
    - ./xampp-linux-x64–7.1.22–0-installer.run

17. Autostart xampp : https://www.computernetworkingnotes.com/linux-tutorials/auto-start-xampp-at-startup-in-ubuntu-linux.html
    - sudo gedit /etc/init.d/lampp
    - In opened file, add the following lines.

        #!/bin/bash
        ### BEGIN INIT INFO
        # Provides: lampp
        # Required-Start:    $local_fs $syslog $remote_fs dbus
        # Required-Stop:     $local_fs $syslog $remote_fs
        # Default-Start:     2 3 4 5
        # Default-Stop:      0 1 6
        # Short-Description: Start lampp
        ### END INIT INFO
        /opt/lampp/lampp start

    - Save it
    - Close the opened file and run the following commands to make it executable.

        $ls –l /etc/init.d/lampp
        $sudo chmod +x /etc/init.d/lampp
        $ls –l /etc/init.d/lampp
    
    - Now run the following command.

        $sudo update-rc.d lampp defaults
        This command instructs Ubuntu to run our script file in all default runlevels.

    - reboot
    
    - Mengatasi Access forbidden !
      1. sudo gedit /opt/lampp/etc/extra/httpd-xampp.conf
      2. pada #New XAMPP security concept ganti/comment :
         Require lokal -> Require all granted
      3. Save, Close
      4. sudo /opt/lampp/lampp restart
      5. isi password di phpmyadmin untuk security
      6. edit password gunakan SQLyog

     - ubah permission lampp jadi open: sudo chmod 777 -R /opt/lampp
       copas repo
     - ubah permission lampp kembali jadi closed: sudo chmod 755 -R /opt/lampp    

18. Install SQLyog
    - Open PlayOnLinux
    - Search SQLyog
    - Install

19. instal firefox 21(sesuaikan versi)
    - wget sourceforge.net/projects/ubuntuzilla/files/mozilla/apt/pool/main/f/firefox-mozilla-build/firefox-mozilla-build_39.0.3-0ubuntu1_amd64.deb
    - sudo dpkg -i firefox-mozilla-build_39.0.3-0ubuntu1_amd64.deb